把已安装插件的菜单聚合到一条指令里，并额外提供"插件介绍"：一次看清各插件到底能做什么。

## 聚合菜单
- 菜单 / 菜单导航 ─ 聚合已安装插件的全部指令（按"所有人可用 / 仅管理员"分组）
- 支持 `menu.md` 与 `metadata.yaml` 的 `menu` 字段两种接入方式，插件无需改代码
- 内容变化才重新渲染（指纹比对），命中缓存直接复用图片

## 插件介绍
- 插件介绍 ─ 逐个插件介绍具体功能，**包含菜单表达不了的"主动触发"能力**（定时推送、后台巡检等）
- 插件介绍 <插件名关键字> ─ 只看某一个插件的介绍（例如 `插件介绍 acmer`）
- 内容来自各插件根目录的 `intro.md`（其次 `metadata.yaml` 的 `intro` 字段），插件自行维护

## 输出与兜底
- 优先渲染成图片；没有可用渲染器时自动回退为纯文本分片
- 渲染失败进入冷却窗口，避免坏环境下每次触发都等超时
- 空态明确提示：未发现插件 / 没有匹配关键字的插件

## 主动行为
- 无定时推送；仅在收到 `菜单` 或 `插件介绍` 时按需生成，并复用上一次的图片缓存

## 配置
- WebUI「菜单导航」页 ─ 选择参与聚合的插件（全部 / 指定插件）
- 保存后立即生效，并自动失效已有图片缓存
## 支持平台
- QQ 官方：qq_official / qq_official_webhook
- QQ 非官方：aiocqhttp（OneBot v11）

## 测试范围
- QQ 官方通道：生产环境长期实测（群内功能与后台页面都已验证）
- OneBot 通道：开发者账号被腾讯风控频繁踢下线，**仅完成小范围真机验证**，其余以代码审查与自动化测试覆盖
