# 更新日志

## [1.2.2] - 2026-09-05

### 修复
- 修复聚合菜单中文字体选择问题；Pillow 回退会显式加载
  `Noto Sans CJK SC` 的简体中文字体面，避免中文显示为方框或字形错乱。
- 升级菜单图片缓存格式，旧字体缓存会自动失效并重新生成。

## [1.2.1] - 2026-09-04

### 优化
- 聚合菜单文字、HTML 和 Pillow 回退统一为爱莉希雅风格的樱粉、珍珠白、
  冰蓝、花瓣与水晶元素。
- 菜单缓存格式升级，确保旧版青绿色菜单图片不会继续复用。

## [1.2.0] - 2026-09-04

### 新增
- 新增 WebUI 配置页，可选择哪些插件参与聚合菜单。
- 默认保持整合全部插件；切换为自选模式后只整合勾选的插件。
- 配置保存后立即生效，并自动使旧菜单图片缓存失效。
- 新增 `最近比赛` 等插件菜单变化会在下一次触发时自动检测并更新。

## [1.1.0] - 2026-09-04

### 变更
- 聚合内容改为提取各插件的菜单指令与简述，避免原样拼接完整 `menu.md` 导致消息过长。
- 菜单指令触发后才检查插件菜单指纹；无变化时复用已有图片，新增或变更插件菜单时
  才重新生成 HTML 并转图发送。
- 转图改为兼容 Chromium/Chrome、Firefox、`wkhtmltoimage` 和 Pillow，不把 Google
  Chrome 作为硬依赖；所有转图方式不可用时才回退为纯文本。

## [1.0.5] - 2026-08-20

### 修复
- 兼容 QQ 官方指令面板自动添加的 `/` 前缀，`/菜单`、`/菜单导航`
  与无前缀写法一致。

## [1.0.4] - 2026-08-17

### 修复
- `repo` 显式指定 `tree/master`：WebUI 更新不再依赖 `api.github.com`
  查询默认分支，避免回退到 `main` 导致 404。

## [1.0.3] - 2026-08-16

### 修复
- `metadata.yaml` 补充 `repo` 仓库地址，修复 WebUI“更新插件”报
  “does not specify a repository URL or download URL”的问题。

## [1.0.2] - 2026-08-16

### 变更
- 聚合菜单展示优化：🧭 标题与更清爽的区块排版。

## [1.0.1] - 2026-08-16

### 新增
- 聚合菜单自动附加各插件的开源仓库链接（读取 `metadata.yaml` 的 `repo` 字段）。

## [1.0.0] - 2026-08-16

### 新增
- 菜单导航插件：发送 `菜单` 即可查看所有已安装插件的聚合菜单。
- 自动扫描各插件目录的 `menu.md`（或 `metadata.yaml` 的 `menu` 字段），
  无需修改插件代码即可接入。
