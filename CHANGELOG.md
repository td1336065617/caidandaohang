# 更新日志

本文件记录所有值得注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

图例：✨ 新增 · 🐛 修复 · ⚙️ 变更 · ⚠️ 升级注意

---

## [1.4.1] - 2026-09-23

> 🐛 Windows 兼容：自动探测系统浏览器与中文字体，不再依赖 Linux 工具链。

### ✨ 新增
- 浏览器候选新增 Windows 常见路径（Chrome / Edge / Firefox 的 Program Files 与 %LOCALAPPDATA% 安装）。
- 中文字体候选新增 Windows 系统字体（msyh.ttc / msyhbd.ttc / simhei.ttf / simsun.ttc / Deng.ttf），Emoji 字体新增 seguiemj.ttf。

### ⚙️ 变更
- 上述候选在非 Windows 平台不存在，会被 shutil.which / Path.is_file 跳过，**Linux/macOS 行为不变**。

---

## [1.4.0] - 2026-09-22

> QQ 双通道适配：支持 OneBot v11（aiocqhttp）与官方 Webhook。

### ⚙️ 变更
- support_platforms 增加 qq_official_webhook、aiocqhttp。
- 纯文本兜底分片上限按通道取值（官方 1500 / OneBot 3000）。

### 🧪 真机验证
- 已用 NapCat（OneBot v11）验证：菜单与插件介绍图片渲染、按通道取分片上限。

---

## [1.3.3] - 2026-09-18

> 📍 修正「插件介绍」条目在菜单里的落点：不再追加到分区末尾。

### 🐛 修复
- 之前把每个插件的 `插件介绍 <插件名>` 条目追加到该插件**分区末尾**，于是像 acmer 这类
  「所有人可用 → 仅管理员」结构的菜单，会把一条人人可用的指令显示在**「仅管理员」分组里**。
- 现在按菜单结构决定落点：**紧跟该插件自己的「XX菜单」条目之后**；没有菜单条目时退到
  **第一个分组的末尾**；两者都保证不会跨进其它分组。

---

## [1.3.2] - 2026-09-18

> 📌 每个插件的菜单分区里补上属于它的「插件介绍」指令。

### ✨ 新增
- 聚合菜单中**每个插件的分区末尾自动追加一条** `插件介绍 <插件名> ─ 查看本插件的完整功能介绍`
  （插件自己已经写了介绍条目的——例如菜单导航自己——不会重复追加）。
- `插件介绍` 新增"插件名 + 后缀"写法：`acmer插件介绍`、`群管理插件介绍`、`表情包插件介绍`
  与 `插件介绍 acmer` 等效，关键字按目录名或显示名匹配（忽略大小写）。

### ⚙️ 变更
- 判定口径统一：**菜单只收录"用户发送后会触发功能"的指令**，`插件介绍` 属于这一类，
  因此既出现在菜单导航自己的分区，也出现在每个插件的分区里；机器人主动推送仍只写在介绍里。

---

## [1.3.1] - 2026-09-18

> 🧭 让「插件介绍」在聚合菜单里可见：菜单导航自己也作为一个分区出现在菜单中。

### ✨ 新增
- 菜单导航根目录新增 `menu.md`：聚合菜单里新增「菜单导航」分区，列出
  `菜单 / 菜单导航` 与 `插件介绍`（含 `插件介绍 {插件名}` 用法）。

### ⚙️ 变更
- 生成菜单时**不再跳过菜单导航自己**，且该分区**不受 WebUI 勾选影响**：
  用户只勾选部分插件时，「插件介绍」入口依然在菜单里（WebUI 插件列表仍跳过自己）。
- 约定明确：**菜单只收录"用户发送后触发功能"的指令**；机器人主动推送（定时早报、
  赛前提醒、赛后推送、后台巡检等）不写进菜单，统一由 `插件介绍` 呈现。

---

## [1.3.0] - 2026-09-18

> 📖 新增 `插件介绍`：把每个插件的具体功能讲清楚，补上菜单表达不了的**主动触发能力**。

### ✨ 新增
- **`插件介绍` 指令**（别名 `功能介绍` / `插件说明` / `插件详情`，全匹配）：
  逐个插件介绍具体功能，包含定时早报、赛前提醒、赛后推送、后台巡检这类
  **不是指令、菜单里看不到**的能力。
- 支持只看一个插件：`插件介绍 acmer`（按目录名或显示名做关键字匹配，忽略大小写）；
  没有匹配时给出明确空态提示。
- 内容约定：插件根目录的 `intro.md`（其次 `metadata.yaml` 的 `intro` 字段），
  都没有时回退该插件的 `desc`/`short_desc`——不写 `intro.md` 也有内容可看。
- 与菜单一致优先渲染图片、失败回退纯文本分片；图片与缓存状态各自独立
  （`intro.png` / `intro_cache.json`），不会与菜单互相顶掉。

### ⚙️ 变更
- 插件介绍与菜单的渲染产物、缓存状态分离：`_render_image()` 新增 `stem` 参数，
  `_load_cached_image()` / `_save_cache_state()` 支持自定义文件名与状态文件（默认值保持菜单行为不变）。
- 与菜单不同，插件介绍**会把本插件自己**也算进去（菜单仍跳过自己）。

### ⚠️ 升级注意
- 无需改配置；各插件是否出现在介绍里取决于它有没有 `intro.md` / `intro` 字段 / `desc`。
- 菜单行为与缓存完全不受影响（`menu.png`、`cache.json` 仍按原样读写）。

---

## [1.2.6] - 2026-09-07

> 坏渲染环境下不再每次触发都等几十秒，纯文本续片更好读，空态文案更明确。

### 🐛 修复
- 渲染失败加入冷却与异常兜底：同一份菜单在 300 秒冷却期内直接回纯文本，不再重复等待超时。
- 缓存图片与新渲染图片增加 PNG 有效性校验（存在、非空、PNG 魔数正确），损坏或空图不再被当作成功图片发送。
- 空态文案区分「暂未发现提供菜单的插件」与「已启用插件均未提供可识别菜单」。

### ⚙️ 变更
- 纯文本续片带 `……（接上条）` 标记，分片优先保留行 / 空格边界。
- 插件显示名折叠多余空白。

---

## [1.2.5] - 2026-09-07

> 修复菜单一级标题漏用 Emoji 字体，图片与 Pillow 回退统一为混合绘制。

### 🐛 修复
- 一级标题 `🌸 菜单导航` 此前未使用 Emoji 字体；HTML/Chromium 与 Pillow 回退现在都走统一的 Emoji 混合绘制。

### ⚙️ 变更
- 升级菜单缓存格式，旧版标题方框图片会自动失效并重新生成。

---

## [1.2.4] - 2026-09-06

> 内置 Emoji 字体，服务器无需安装字体或浏览器也能正常显示 Emoji。

### ✨ 新增
- 插件内置 Noto Color Emoji 字体（`assets/fonts/NotoColorEmoji.ttf`），HTML 渲染与 Pillow 回退都可使用，不要求服务器安装字体、安装系统软件或提供 Google Chrome。

### 🐛 修复
- 菜单标题和分组中的 Emoji 不再直接交给中文字体绘制，避免服务器缺少 Emoji 字形时出现「方框里的 X」。
- Pillow 无法按小字号直接加载彩色 Emoji 时，改为先以高分辨率绘制再缩放，并让换行测量与实际混合绘制保持一致。
- Chromium 渲染允许加载菜单 HTML 同目录的内置字体文件。

---

## [1.2.3] - 2026-09-05

> 放宽字间距、行距与卡片留白，中文菜单不再挤成一团。

### ⚙️ 变更
- 放宽聚合菜单的字间距、行距和插件卡片留白，改善中文菜单过密的问题。
- HTML 与 Pillow 回退保持一致，并升级菜单缓存格式，旧版图片会自动重绘。

---

## [1.2.2] - 2026-09-05

> 显式加载简体中文字体面，修复中文方框与字形错乱。

### 🐛 修复
- 修复聚合菜单中文字体选择问题：Pillow 回退会显式加载 `Noto Sans CJK SC` 的简体中文字体面，避免中文显示为方框或字形错乱。

### ⚙️ 变更
- 升级菜单图片缓存格式，旧字体缓存会自动失效并重新生成。

---

## [1.2.1] - 2026-09-04

> 聚合菜单统一为樱粉 / 珍珠白的爱莉希雅风格配色。

### ⚙️ 变更
- 聚合菜单文字、HTML 与 Pillow 回退统一为樱粉、珍珠白、冰蓝、花瓣与水晶元素的视觉风格。
- 升级菜单缓存格式，确保旧版青绿色菜单图片不会继续复用。

---

## [1.2.0] - 2026-09-04

> 新增 WebUI 配置页，可以选择哪些插件参与聚合。

### ✨ 新增
- 新增 WebUI 配置页，可选择哪些插件参与聚合菜单。
- 默认保持整合全部插件；切换为自选模式后只整合勾选的插件。
- 配置保存后立即生效，并自动使旧菜单图片缓存失效。
- 各插件菜单（例如 `最近比赛`）发生变化时，会在下一次触发时自动检测并更新。

---

## [1.1.0] - 2026-09-04

> 聚合内容改为「指令 + 简述」，并按需渲染、兼容多种转图方式。

### ⚙️ 变更
- 聚合内容改为提取各插件的菜单指令与简述，避免原样拼接完整 `menu.md` 导致消息过长。
- 菜单指令触发后才检查插件菜单指纹；无变化时复用已有图片，新增或变更插件菜单时才重新生成 HTML 并转图发送。
- 转图兼容 Chromium/Chrome、Firefox、`wkhtmltoimage` 与 Pillow，不把 Google Chrome 作为硬依赖；所有转图方式都不可用时才回退为纯文本。

---

## [1.0.5] - 2026-08-20

> 兼容 QQ 官方指令面板自动添加的 `/` 前缀。

### 🐛 修复
- `/菜单`、`/菜单导航` 与无前缀写法行为一致。

---

## [1.0.4] - 2026-08-17

> `repo` 显式指定 `tree/master`，WebUI 更新不再依赖 GitHub API。

### 🐛 修复
- `repo` 显式指定 `tree/master`：WebUI 更新不再依赖 `api.github.com` 查询默认分支，避免回退到 `main` 导致 404。

---

## [1.0.3] - 2026-08-16

> 补充 `repo` 字段，修复 WebUI 一键更新报错。

### 🐛 修复
- `metadata.yaml` 补充 `repo` 仓库地址，修复 WebUI「更新插件」提示 `does not specify a repository URL or download URL` 的问题。

---

## [1.0.2] - 2026-08-16

> 聚合菜单展示优化。

### ⚙️ 变更
- 标题与区块排版调整，展示更清爽。

---

## [1.0.1] - 2026-08-16

> 聚合菜单自动附上各插件的开源仓库链接。

### ✨ 新增
- 聚合菜单自动附加各插件的开源仓库链接（读取 `metadata.yaml` 的 `repo` 字段）。

---

## [1.0.0] - 2026-08-16

> 首个版本：发送 `菜单` 即可查看所有已安装插件的聚合菜单。

### ✨ 新增
- 菜单导航插件：发送 `菜单` 即可查看所有已安装插件的聚合菜单。
- 自动扫描各插件目录的 `menu.md`（或 `metadata.yaml` 的 `menu` 字段），无需修改插件代码即可接入。
